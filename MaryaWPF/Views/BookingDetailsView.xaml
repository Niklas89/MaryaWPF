<UserControl x:Class="MaryaWPF.Views.BookingDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MaryaWPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Orientation="Vertical">
            <!--<TextBlock x:Name="Header" FontSize="20" FontWeight="Bold" Margin="10" />
            <TextBlock x:Name="Message" FontSize="18" Margin="10" />-->

            <ListBox x:Name="Bookings" Grid.Row="2" Grid.Column="0" MinHeight="200" 
                 MinWidth="150">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Vertical">
                            <TextBlock Text="{Binding Description}"/>
                            <StackPanel Orientation="Horizontal">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="Price: " FontSize="14"/>
                                    <TextBlock Text="{Binding TotalPrice, ConverterCulture='fr-FR', StringFormat='{}{0:C}'}" FontSize="14"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="5 0 0 0">
                                    <TextBlock Text="Qty: " FontSize="14"/>
                                    <TextBlock Text="{Binding AppointmentDate}" FontSize="14"/>
                                </StackPanel>
                            </StackPanel>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <Button x:Name="CancelBooking" Padding="5" Margin="20">Annuler la réservation</Button>
            <Button x:Name="Close" Padding="5" Margin="20">Fermer</Button>
            <Button
              Width="150" Margin="0 0 0 0"
              Content="Modifier" x:Name="Edit"
              Visibility="{Binding IsEditVisible, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}" />
        </StackPanel>
    </Grid>
</UserControl>
